PORT=3000
DATABASE_URL=postgresql://closy:closy@localhost:5432/closy
GEMINI_API_KEY=your-gemini-api-key
OPENAI_API_KEY=your-openai-api-key
# Publicly reachable address of this server, used to build links to uploaded
# images. Update this whenever that address changes (new ngrok session, real
# deployment) — no code change or data migration needed.
PUBLIC_ASSET_BASE_URL=http://localhost:3000
